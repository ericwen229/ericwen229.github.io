# 历史文章归档

`_posts/` 保存重新初始化前的全部 5 篇文章，保留原始文件名与内容。
`Gemfile.lock` 保存旧站点的依赖记录，不用于新站点构建。

归档目录已通过根目录 `_config.yml` 的 `exclude` 排除，不会生成页面或复制到发布产物中。
如需重新发布某篇文章，将其移回根目录 `_posts/`，并确保文件名为 `YYYY-MM-DD-title.markdown`。
